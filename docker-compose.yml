services:
  mareate-app:
    build: .
    container_name: mareate-app
    ports:
      - "80:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - FRONTEND_URL=http://43.160.253.32
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./temp:/app/temp
    restart: unless-stopped
    networks:
      - mareate-network

  nginx:
    image: nginx:alpine
    container_name: mareate-nginx
    ports:
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - mareate-app
    restart: unless-stopped
    networks:
      - mareate-network

networks:
  mareate-network:
    driver: bridge 